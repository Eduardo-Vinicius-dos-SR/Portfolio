

export default function Contact({active = false}) {
    return(
        <section id="contact" className={`${active ? 'flex' : 'hidden'} flex-col items-center justify-center gap-4 p-6 text-center`}>

        </section>
    )
}